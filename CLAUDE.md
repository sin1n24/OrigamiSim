# CLAUDE.md

このプロジェクトの目的とワークフローについて、Claude Code(特にOpus)向けの指針。

## 背景

以下の記事のアイデアを元にしたゲーム開発プロジェクト。
https://zenn.dev/koher/articles/code-first-3d-modeling

記事の要旨: Blenderなどのツールに頼らず、**コードだけで3Dモデルを作り、AIが直接コードを書いて反復調整する**。記事のオリジナル実装は Swift + RealityKit + 自作ライブラリ Realitizer(`extrude → beveled → recalculateNormals` のメソッドチェーン)。

RealityKit は Apple専用(macOS/Xcode必須)でこの開発環境(Windows)では使えないため、**Three.js + TypeScript + Vite** で同じ考え方を再現している。

`src/modeling/builder.ts` は記事本来の extrude/bevel API を模したもの(現状未使用)。`.beveled({ width })` は輪郭を内側に削るのではなく全方向に `width` 分外側に拡張することが実測済み。

## 現在のプロジェクト: 折り紙シミュレーター

**方向転換の経緯**: 最初は「展開図を自分の手で折って立体を組み立てる箱パズル」を作ったが、ユーザーから「箱は面白くない。チューリップ/かぶと/コップなど本物の折り紙を、パズルというより折り紙シミュレーターとして」という指示で方向転換。さらに「独自の折り方を発明せず、実在する折り図(山折り・谷折りの線入り)を参照すること」という明示的な制約が入った。旧・箱パズルの実装は`DELETE/`に退避済み(下記参照)。

コップだけで一度ゲームとして完成させた後、ユーザーから「もっとシンプルでも良いので、コップ以外も増やして。山とかどう？目標4つ」と指示があり、検討の結果 **紙コップ・きつねの顔・ヨット・紙飛行機・山** の5モデルを実装した(2026-09-16)。当初候補にあった奴さん(やっこさん)は実在の折り図を確認した結果スクワッシュ折り必須と判明し、ユーザーの了承を得て見送った。

### 到達点(2026-09-16時点)

**5モデル、ガイド付き折り紙シミュレーターとして完成**:
- **紙コップ**: The Japan Society & Japan Foundation (KD&TN) "Origami Paper Cup" 図解に準拠。5ステップ。完成後、正面レイヤーのヒンジを180°→115°に開き直し、平坦な折り紙が実際の器の形に開く演出。
- **きつねの顔**: origamijapan.net/fox-face/ の6コマ図解(うち2つは参照クレースと装飾で実際の折りは4ステップ)に準拠。平坦なまま完成。
- **ヨット**: origamisho.com の「Type1(スクワッシュ不要な簡易版)」の実物写真連番に準拠。3ステップ+開いて帆を立てる演出。
- **紙飛行機**: origamisho.com の正方形折り紙ダーツの実物写真連番に準拠。6ステップ、伝統的な「両上角を中心へ→再度中心へ→半分に折る→翼を折る」構成。
- **山**: 参照図なし(単純な対角線折り+再展開のみで曖昧さがないため)。対角線に1回折り、そのヒンジを180°→80°に開き直してテント状に立てる。

各モデルの正確な折り目座標の由来・設計判断は下記「実装済みモデルの詳細」を参照。

**保留(実装不可、現エンジンの限界)**:
- **かぶと(兜)**: David Mitchell, origamiheaven.com "Kabuto or Samurai Helmet"。基本形(step1〜11)までは平坦折りで作れるが、step12の"スクイーズして中心を前後に開く"操作がスクワッシュ折り(1枚のフラップを2枚に割って左右対称に開く、レイヤー位相そのものが変わる操作)で現エンジンでは表現不可。
- **チューリップ**: 同様にスクワッシュ/花弁折りが前提で実現できない。
- **奴さん(やっこさん)**: 実在の折り図(kinki.kuwv.org のPDF)を確認したところ、腕・足(および上半身の着物部分だけでも)を作る「袴折り」がスクワッシュ折りで、triple-blintzだけ止めても人型に見えず断念。ユーザーに報告し了承済み(2026-09-16)。
- **ヨット/ボート(立体的なもの)**: origamijapan.net の「だまし舟」系(9ステップ・7ステップ版)は中央のひし形部分を指で開いて3D化する操作や、内側反転折りに相当する「回転」記号があり、いずれもスクワッシュ/リバースフォールド系で不可。**簡易版(Type1)のみ採用**。

**この3つ(かぶと・チューリップ・奴さん)をユーザーに「できる」と誤って約束しないこと。** スクワッシュ折り対応は既存の「クリップ&回転」エンジンへの本格的な機能追加であり、着手する場合は必ず事前にユーザーと相談すること(advisorの明示的指示)。

### 折り紙エンジンのアーキテクチャ

- `src/origami/geometry.ts` — `Pt`型、`signedSide`(直線に対する符号付き位置)、`clipHalfPlane`(Sutherland–Hodgmanによる半平面クリップ。折り目の直線でポリゴンを「残る側」「動く側」に分割する中核処理)、`layerGeometry`(2DポリゴンからTHREE.ExtrudeGeometryで薄い紙のメッシュを作る)、`polygonArea`。
- `src/origami/affine.ts` — 2Dアフィン変換`{a,b,c,d,tx,ty}`。`compose`/`apply`/`invert`/`translation`/`rotation`、および`MIRROR_Y`(180°の平坦折りが子レイヤーのローカル座標系をY軸反転させることを表す)。
- `src/origami/foldEngine.ts` — `OrigamiModel`クラス。紙を「レイヤーの配列」として表現し、`fold(step: FoldStep)`を呼ぶたびに対象レイヤーを折り目の直線でクリップして「残る側(kept)」と「動く側(moved=新しいレイヤー)」に分ける。`fold()`はそのステップで新規に作られたヒンジのインデックス配列を返す(後から`setHingeTarget()`で角度を変更するため)。他に`previewCrease(step)`(次の折り目を赤線で表示するガイド)、`setHingeTarget(hingeIndex, angleDeg)`(既存ヒンジの目標角度を後から変更。「完成後に開く」演出で使用)、`dispose()`(リセット時のジオメトリ解放)。
- `src/origami/models.ts` — `ModelDef`(`{id, name, steps, openReveal?}`)の配列`MODELS`。各モデルのビルダー関数(`buildCup()`など)が折り目座標を計算して返す。`diamond()`ヘルパー(ダイヤモンド座標系→エンジン座標系の変換)と`perpBisector()`(2点の垂直二等分線)を共有。

### ゲームUI(`src/main.ts`)

`models.ts`の`MODELS`配列からモデルを選ぶ画面(`selectOverlay`)を最初に表示し、選択後にそのモデルの`steps`を順に折っていく状態機械。**`fold()`は不可逆**(`layer.poly`を破壊的にクリップし、ジオメトリをdisposeする)なので、「やり直す」は折りを巻き戻すのではなく`OrigamiModel`を作り直して`stepIndex=0`から再生する(`startModel(def)`)。

- クリックで1ステップ進む(`advanceStep()`)。`pointerdown`→`pointerup`の距離6px閾値でOrbitControlsのドラッグとクリックを区別する(旧・箱パズルからの実証済みパターンを継承)。
- 各ステップの直前に`model.previewCrease()`で次の折り目を赤線表示し、折ったら`disposePreview()`で消してから次のガイドを出す。
- `stepHinges: number[][]`に各ステップの`fold()`戻り値(ヒンジインデックス配列)を蓄積しておき、完成時に`currentModelDef.openReveal`(`{stepIndex, hingeSlot, angleDeg}[]`)を参照して該当ヒンジを`setHingeTarget()`で開き直す。
- 完成で「できあがり!」オーバーレイ(`.win-overlay`、表示切替は`el.style.display`で行う。**`hidden`属性は使わない**、下記「ハマった点」6参照)。「もう一度折る」(同モデル再挑戦)と「他のモデルを選ぶ」(選択画面に戻る)。HUDにも常時「モデルを選ぶ」ボタンあり。
- `window.__debug = { model(getter), currentModelDef(getter), camera, controls, THREE, advance, reset, select, startModel, MODELS }` を検証用に公開。ヒンジは`model['hinges'].forEach(h=>{h.current=h.target; h.group.rotation.x=h.current})`で強制的に収束させてからスクリーンショットを撮ると、rAF停滞(下記「ハマった点」5)の影響を受けずに確認できる。

### 核心的な設計: ルート座標系での一括作図(`localToRoot`)

折り目は毎回「今操作しているレイヤー自身のローカル座標」ではなく、**最初の正方形(ルート)の座標系で一度だけ**書けるようにしてある。各レイヤーは`localToRoot`(そのレイヤー独自の2D座標→ルート座標への変換)を保持し、`fold()`内部で`invert(layer.localToRoot)`を使って、渡された座標をそのレイヤー自身のローカル座標に変換してからクリップする。

**180°の平坦折りは子レイヤーのローカル座標系をミラー(Y軸反転)する。** これを`localToRoot`の合成に`MIRROR_Y`として明示的に含めないと、折り目チェーンの中で奇数回180°折られた深さのレイヤーすべてでクリップの符号判定が壊れる。これが最初に踏んだ大きなバグだった。

**`movingSidePoint`パターン**: 呼び出し側が生の`+1/-1`のクリップ符号を渡すのではなく、「動くべき側にあることが分かっているルート座標系の1点」を渡す。エンジン側でその点を対象レイヤーのローカル座標に変換し、`signedSide`から正しい符号を導出する。ミラーされたフレームでは生の符号が安定しないため、この間接化が必須。

### 座標系はモデルごとに異なる(「ダイヤモンド」frame と「ネイティブ」frame)

`OrigamiModel`の初期正方形は角が`(±0.5,±0.5)`の**軸平行**な正方形(ネイティブ frame)。だが折り図が正方形を45°回転した「ダイヤモンド」置き(角が上下左右を向く)で描かれていることが多く、その場合は角がN,E,S,W(軸上、半径0.5)の「ダイヤモンド frame」で座標を書いた方が図と対応が取りやすい。`models.ts`の`diamond()`関数(`(x,y) → (x-y, x+y)`、45°回転+√2倍スケールを一発でやる式)でダイヤモンド→ネイティブに変換する。

**どちらのframeで書いたかはモデルごとに明示すること**:
- ダイヤモンドframe(要`diamond()`変換): 紙コップ、きつねの顔、ヨット — いずれも元図が正方形を角が上下左右を向くよう置いている。
- ネイティブframe(`diamond()`を使わない): 紙飛行機 — 元図が普通の(辺が水平垂直の)正方形置き。奴さん(やっこさん、実装は見送ったが)のブリンツ折りも本来これに該当する。

新しいモデルを追加するときは、参照図がどちらの置き方かを確認し、`models.ts`の該当ビルダー関数の先頭コメントに明記すること。混同すると全く違う形をクリップしてしまう(下記「ハマった点」1)。

### ハマった点まとめ(将来また踏まないように)

1. **座標系の罠(コップ実装で発覚、丸1セッション分溶かした)**: 上記「座標系はモデルごとに異なる」参照。ダイヤモンドframeの座標をそのまま`fold()`に渡すと全く違う形(長方形分割など)をクリップしてしまっていた。
2. **Three.jsのEuler合成順序の罠(コップ実装で発覚、最大のバグ)**: 1つの`THREE.Group`に`rotation.z`(向き固定)と`rotation.x`(折りアニメーション)を両方セットすると、直感("まずzで軸を向けてからxで回す")とは逆に、実際は`Rx`が先(内側)・`Rz`が後(外側)で合成される。単発の折り(ルート直下の子)ではたまたま気づかないことがあるが、既に折られたレイヤーの上にさらに折りを重ねる(孫階層)と数値が完全に破綻する。**修正**: 1つの`Group`に2軸を混在させず、`orientGroup`(位置+`rotation.z`のみ、固定)と、その子の`foldGroup`(`rotation.x`のみ、アニメーション対象)の**2段ネスト**に分離した。子をどちらに追加するかは常に`foldGroup`側(`layer.group`は常に`foldGroup`を指す)。今後3軸以上絡む変換が必要になっても、複数軸を1つのEulerに混在させず、単軸グループを重ねる方式を続けること。
3. **クリップの縮退(かぶと・コップ双方で発覚)**: 複数のレイヤーに同時に`targetLayers:'all'`で折り目を適用すると、既に一度クリップされた頂点が新しい折り目の直線上にちょうど乗るケースがあり、`clipHalfPlane`が面積ゼロの縮退ポリゴン(1点に潰れた"レイヤー")を生成することがある。`movingPoly.length<3`という頂点数チェックだけでは検出できない(縮退頂点が3個以上生成されることがある)。**修正**: `foldEngine.ts`・`verify/engine.ts`ともに、`movingPoly`と`keptPoly`の両方に**面積ベースのイプシロン判定**(`polyAreaAbs(poly) < 1e-9`)を追加。`movingPoly`が縮退→そのレイヤーは折り目が届いていないので無変更。`keptPoly`が縮退→レイヤー全体が「動く側」(wholesale move)なので、古いメッシュを破棄してゴーストレイヤーを残さないよう分岐している。
4. **`angleDeg`が負の180°(-180)だとミラーが効かないバグ**: `isFlatHalfTurn`の判定が元々`Math.abs(step.angleDeg - 180) < 1e-6`だったため、`-180`(逆向きに180°折る、実際には同じ物理的な折りだが回転アニメーションの向きが逆)だと判定に失敗し、`MIRROR_Y`が合成されずレンダリングは正しく見えても`localToRoot`のブックキーピングだけ壊れる(後続のfoldで初めて症状が出る)、というMIRROR_Yバグと同種の潜在バグだった。`Math.abs(Math.abs(step.angleDeg) - 180) < 1e-6`に修正。修正後、紙コップの数値検証(`verify/cup.ts`)が修正前と完全に同じ結果になることを確認済み(回帰テスト)。
5. **オブリークカメラでの見誤り**: 3/4視点(斜め)だと、複数の重なったレイヤーがパースの歪みで「重なっていない」ように見えることがある。真上からのカメラ(`camera.position.set(0,3,0.001)`; `lookAt(0,0.4,0)`)で確認するのが確実。`main.ts`のデフォルトカメラも`(0.8,2.4,1.2)`という比較的急な角度に設定済み。
6. **自動化ブラウザでのrequestAnimationFrame停滞、および稀に画面幅0でスクリーンショット失敗**: Chrome拡張(claude-in-chrome)経由でスクリーンショットを撮る際、`requestAnimationFrame`ベースのアニメーションが実時間通りに進まないことがある(ヒンジの`current`が`target`に収束しきらない状態で固まる)。折り目が完全に閉じた状態を確認したい場合は、`__debug.model['hinges'].forEach(h=>{h.current=h.target; h.group.rotation.x=h.current})`のように強制的に目標角へ飛ばしてから確認する。また、このセッションでは`Page.captureScreenshot`が"Cannot take screenshot with 0 width"で断続的に失敗する現象があった(タブ再作成でも治らないことがある)。その場合は`THREE.Box3().setFromObject(model.root)`で境界ボックスを数値的に確認する方法に切り替えるとよい(全5モデルでこの方法により座標の健全性を確認済み)。ブラウザの`computer`ツールの`left_click_drag`はOrbitControlsを動かすのに必要な中間の`pointermove`イベントを発火しないため、ドラッグ操作の検証には`PointerEvent`を`pointerdown`→複数回の`pointermove`→`pointerup`で手動発火させること。
7. **CSSの罠**(旧・箱パズル時代の教訓、汎用的に有効): `.foo{display:flex}`のようにクラスで`display`を指定すると`[hidden]{display:none}`のデフォルトを上書きし、`el.hidden=true`が効かなくなる。`el.style.display`で明示的に切り替えること。
8. **紙の厚み(ExtrudeGeometry)が「断面が切れて見える」バグの原因だった**: `layerGeometry()`は元々`THREE.ExtrudeGeometry`で紙に実際の厚み(0.012)を持たせていたが、押し出しソリッドは折り目の切断面で側面(側壁)が露出し、2枚重なったレイヤーの断面がずれて見える(まるで切り取られたよう)原因になっていた。**修正**: `THREE.ShapeGeometry`(厚みゼロの平面)に変更し、レイヤー間のz方向オフセットも見た目上の厚みではなくz-fighting防止だけの微小値(`Z_EPSILON = 0.0003`、旧`PAPER_THICKNESS`から改名)にした。紙の厚みを表現したくなったときは、押し出しではなく別の方法(法線オフセットしたシェルなど)を検討すること。
9. **完成後の鑑賞を妨げるフルスクリーンオーバーレイ**: 完成時の`.win-overlay`が`inset:0`の全画面`div`で、`pointer-events`を制限していなかったため、背景が半透明でモデルが見えていてもOrbitControlsのドラッグ操作(カメラ回転)を画面全体で奪ってしまい、「完成したモデルを眺め回す」ことができなかった。**修正**: オーバーレイのラッパーを`pointer-events:none`にして画面下部に寄せ、カード自体(`.win-card`)だけ`pointer-events:auto`にした。今後モーダル的なオーバーレイを追加するときは、3D操作を妨げていいのかどうかを毎回意識すること。

### 実装済みモデルの詳細(`src/origami/models.ts`)

- **紙コップ (`buildCup`)**: [The Japan Society & Japan Foundation "Origami Paper Cup" (KD&TN)](https://www.japansociety.org.uk/rsn/lessons/resources/RSN-Resources-OrigamiCup.pdf)。ダイヤモンドframe。左右の角を折る2ステップの折り目座標は、図の2つの明示的な制約("反対側の辺に届く"/"底辺に平行")から連立方程式で数値的に逆算したもの(`verify/cup.ts`参照)。名古屋市港防災センターPDF等、別解説でも同じ伝統モデルであることを裏取り済み。
- **きつねの顔 (`buildFox`)**: [origamijapan.net/fox-face/](https://origamijapan.net/fox-face/) の6コマ写真。ダイヤモンドframe。ステップ構成(半分折り→参照クレース[スキップ]→頂点折り→耳2つ)は図の通りだが、耳の折り目の正確な比率は図・origamiway.comの説明("中心まで完全には折らない")のどちらにも数値がなく、設計判断として選んだ(耳の先が本体の輪郭からはみ出し、かつ顔が大部分残ることを`verify/fox.ts`で確認済み)。
- **ヨット (`buildYacht`)**: [origamisho.com](https://origamisho.com/archives/1839) の「Type1(簡易版、スクワッシュ不要と明記)」の実物写真連番。ダイヤモンドframe。船首の折り目座標は写真から寸法を特定できなかったため設計判断(`verify/yacht.ts`)。完成後、半分折りのヒンジを180°→130°に開き直して帆を立てる。
- **紙飛行機 (`buildDart`)**: [origamisho.com](https://origamisho.com/archives/1258) の正方形折り紙ダーツの実物写真連番。**ネイティブframe**(`diamond()`を使わない)。両角を中心へ折る操作の折り目(`TC`-`LC`など)は「直角二等辺三角形の斜辺で頂点を反射する」ことから厳密に導出。2回目の"先端を細くする"折り目のx座標(`√2/2 - 0.5`)は紙コップの頂点折りと**全く同じ定数**(どちらも「辺を中心線に重ねる」幾何なので偶然ではない)。翼の折り目角度は実機の飛行性能に関わる部分で写真でも数値が読み取れず設計判断。
- **山 (`buildMountain`)**: 参照図なし。対角線1本を谷折りしてそのヒンジを180°→80°に開き直すだけの単純な構成で、曖昧さがないため参照図を必要としないとadvisorの助言あり。

### 検証方法

1. **数値検証(最優先・高速)**: `verify/engine.ts`に、`OrigamiModel`を使わず素のデータ(2D配列+アフィン変換)だけで`fold()`をシミュレートする最小実装(`fold`/`area`/`toRoot`/`dedupe`/`diamond`/`freshLayers`/`report`)がある。**レイヤーのインデックス方式は本物のエンジンと完全一致させてあること**(`kept`は同じインデックスのまま、`moved`は全ターゲット処理後にまとめて配列末尾に追加。これを一度、単純に「kept/movedを1レイヤーごとに交互にpush」で実装してしまい、`targetLayers:[番号]`を使うモデルで別のレイヤーを検証してしまうバグを埋め込んだことがある)。モデルごとに`verify/<name>.ts`(`cup.ts`/`fox.ts`/`yacht.ts`/`dart.ts`)を作り、`node --experimental-strip-types verify/<name>.ts`で1秒未満で実行、各ステップ後の面積・頂点座標を確認する。新しい折り目を追加するときは、まずここで検証してから`models.ts`に移植する。ブラウザでの目視確認より圧倒的に速く確実。
2. **ブラウザでの3D確認**: `npm run dev -- --port 5173 --strictPort`(バックグラウンド起動)。`javascript_tool`で`window.__debug`(上記参照)を使い、`startModel(def)`→`advance()`を必要回数→ヒンジ強制収束、の順で任意のモデルを任意の段階まで進められる。スクリーンショットが取れない場合は上記「ハマった点」6の`Box3`による数値確認に切り替える。

### 未使用ファイル / 削除ポリシー

このユーザーのルールでは**不要ファイルは削除せずDELETEフォルダへ移動する運用**。

- `DELETE/src-scenes/foldPrototype.ts`、`DELETE/src-game/foldGame.ts`、`DELETE/src-game/levels.ts` — 旧・箱パズルゲームの実装一式(方向転換前)。ゲームロジック(手数記録・確定式の答え合わせ・孫ヒンジの位置計算など)は動いていたが、内容の設計判断(方向の曖昧性がない)により置き換えられた。参考にする場合はここを見る。
- `DELETE/scratch-solve.ts`、`DELETE/scratch-verify-main.ts` — コップの折り目座標を数値的に逆算した使い捨てスクリプトと、座標系バグ(上記「ハマった点」1)を切り分けるための一時デバッグスクリプト。役目は終わっているが、導出過程の記録として残置。
- `src/counter.ts`, `public/vite.svg`, `src/assets/typescript.svg` — `npm create vite`のボイラープレート残骸。実質的な影響はない。

### 開発ワークフロー

1. 新しい折り目ステップを追加するときは、まず`verify/<モデル名>.ts`で数値検証してから`src/origami/models.ts`に`build<モデル名>()`関数として移植する。
2. `npm run dev -- --port 5173 --strictPort`で開発サーバー起動。メモリ逼迫でViteが落ちることがあるので、`curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5173`で生存確認してから使うこと。
3. `npx tsc --noEmit`で型チェック。
4. ブラウザ確認は上記「検証方法」参照。

### 今後やるとしたら

- **モデル追加**: `src/origami/models.ts`に`build<Name>()`関数を追加し、`MODELS`配列に加えるだけでUIに自動的に反映される(モデル選択画面・ステップ進行・完成演出はすべて`ModelDef`のデータだけで動くようになっている)。追加前に必ず実在の折り図を確認し、スクワッシュ/花弁折りが必要かどうかを`verify/`で数値検証する前にまず図を見て判断すること(平坦折りだけで完結するかを見極める)。
- フラップを既存レイヤーの**下に**差し込む("tuck under")表現(`FoldStep`に明示的なZスタック順の上書きフィールドを足す)は、より複雑なモデル(ポケットに差し込む系の折り方)で必要になる可能性がある。現状は常に新しいフラップが最前面に積まれる単純なスタッキングのみ。
- スクワッシュ折り対応(かぶと・チューリップ・奴さんの前提)は、レイヤーを2つに割って左右対称に開く+スタック順の並べ替えが必要な、本格的な機能追加。着手する場合は必ず事前にユーザーと相談すること。
